# 蜂鸟视图标准语义地图输出 v0.2

> Schema 版本：0.2  
> 文件包类型：`FMAP_SEMANTIC_MAP`   
> 坐标参考系：EPSG:3857

## 1. 文档范围

本文定义标准语义地图测试数据集文件包的目录、文件格式和字段含义。文件包用于空间数据查询、关系分析、导航计算以及第三方系统集成。

本文使用以下输出规则：

- **固定输出**：每个文件或对象都会输出该字段；
- **有值时输出**：存在有效业务值时输出，没有有效值时省略；
- 对象或数组能否为空，在对应字段说明和约束中单独说明。

所有 JSON 和 JSONL 文件均使用 UTF-8 编码。JSONL 文件采用一行一条完整 JSON 记录的格式。

## 2. 文件包结构

每张地图使用独立目录，目录名与 `mapId` 一致：

```text
{mapId}/
├── manifest.json
├── entities.json
├── relations.jsonl
└── navigation/
    ├── nodes.jsonl
    └── edges.jsonl
```

| 文件 | 格式 | 数据粒度 | 作用 |
| --- | --- | --- | --- |
| `manifest.json` | JSON | 每张地图 1 个 | 描述文件包版本、地图、坐标系、楼层、数据规模和文件完整性信息 |
| `entities.json` | JSON | `entities` 数组中每项 1 个实体 | 表达楼层、空间、设施、跨层设施和导航路径及其几何 |
| `relations.jsonl` | JSONL | 每行 1 条关系 | 表达实体之间的包含、归属、服务和连接关系 |
| `navigation/nodes.jsonl` | JSONL | 每行 1 个节点 | 表达导航图中的路径节点、跨层设施节点和空间接入点 |
| `navigation/edges.jsonl` | JSONL | 每行 1 条有向边 | 表达楼层内路径和跨楼层换乘连接 |

没有导航数据的地图仍包含 `navigation/nodes.jsonl` 和 `navigation/edges.jsonl`，两个文件内容为空，对应记录数为 0。

### 2.1 目录与文件约定

- 地图根目录名称固定为 `mapId`，文件名和目录名区分大小写；
- 文件路径使用 `/` 分隔，不使用绝对路径，不包含 `..` 路径段；
- 文件包只包含本章定义的文件和目录；

### 2.2 压缩包约定

测试集使用 ZIP 形式，压缩文件名称采用：

```text
{mapId}-semantic-v{schemaVersion}.zip
```

例如，地图 `911907` 的 v0.2 文件包命名为 `911907-semantic-v0.2.zip`。压缩包内只包含一个 `{mapId}/` 根目录，根目录内结构与本章定义一致。

## 3. manifest.json

### 3.1 文件作用

`manifest.json` 是语义地图文件包的入口，用于：

- 识别 Schema 版本和文件包类型；
- 识别地图及其语义 ID；
- 声明几何坐标参考系；
- 获取楼层和各类数据的数量；
- 发现包内数据文件；
- 提供文件大小和 SHA-256 完整性信息。

### 3.2 顶层字段

| 字段 | 类型 | 输出规则 | 示例 | 含义 |
| --- | --- | --- | --- | --- |
| `schemaVersion` | String | 固定输出 | `"0.2"` | 当前文件包采用的 Schema 版本 |
| `format` | String | 固定输出 | `"FMAP_SEMANTIC_MAP"` | 文件包类型标识 |
| `packageProfile` | String | 固定输出 | `"DISTRIBUTION"` | 对外交付 Profile，固定为 `DISTRIBUTION` |
| `mapId` | String | 固定输出 | `"911907"` | 地图业务标识，与地图目录名称一致 |
| `semanticId` | String | 固定输出 | `"map:911907"` | 地图语义 ID，可被地图级关系引用 |
| `coordinateReferenceSystem` | Object | 固定输出 | 见 3.3 | 所有实体和导航几何使用的坐标参考系 |
| `floors` | Array&lt;FloorSummary&gt; | 固定输出 | 见 3.4 | 楼层清单及每层实体数量；没有楼层时为空数组 |
| `recordCounts` | Record&lt;String, Integer&gt; | 固定输出 | 见 3.5 | 主要记录型文件的记录总数 |
| `entityCounts` | Record&lt;String, Integer&gt; | 固定输出 | 见 3.6 | 按实体类型统计数量 |
| `relationCounts` | Record&lt;String, Integer&gt; | 固定输出 | 见 3.7 | 按关系类型统计数量 |
| `files` | Array&lt;FileMetadata&gt; | 固定输出 | 见 3.8 | 包内数据文件及其完整性信息 |

### 3.3 coordinateReferenceSystem

本次测试数据集几何坐标统一采用 EPSG:3857：

```json
{
  "authority": "EPSG",
  "code": 3857,
  "name": "WGS 84 / Pseudo-Mercator",
  "axisOrder": ["x", "y"],
  "unit": "metre"
}
```

| 字段 | 类型 | 输出规则 | 固定值 | 含义 |
| --- | --- | --- | --- | --- |
| `authority` | String | 固定输出 | `"EPSG"` | 坐标参考系编码机构 |
| `code` | Integer | 固定输出 | `3857` | EPSG 坐标参考系代码 |
| `name` | String | 固定输出 | `"WGS 84 / Pseudo-Mercator"` | 坐标参考系名称 |
| `axisOrder` | Array&lt;String&gt; | 固定输出 | `["x", "y"]` | 坐标轴顺序 |
| `unit` | String | 固定输出 | `"metre"` | X、Y 坐标和平面距离的单位 |

### 3.4 floors

每个楼层元素采用以下结构：

```json
{
  "name": "F1",
  "gid": 2,
  "alias": "一层",
  "entityCount": 593
}
```

| 字段 | 类型 | 输出规则 | 含义 |
| --- | --- | --- | --- |
| `name` | String | 固定输出 | 楼层名称，例如 `B1`、`F1` |
| `gid` | Integer | 固定输出 | 楼层顺序或业务编号；不是语义实体 ID |
| `alias` | String | 有值时输出 | 楼层展示别名；空字符串不输出 |
| `entityCount` | Integer | 固定输出 | 该楼层全部语义实体数量，包含楼层自身的 `Floor` 实体 |

### 3.5 recordCounts

```json
{
  "entities": 2648,
  "relations": 3047,
  "navigationNodes": 1437,
  "navigationEdges": 3426
}
```

| 字段 | 类型 | 输出规则 | 含义 |
| --- | --- | --- | --- |
| `entities` | Integer | 固定输出 | `entities.json` 中 `entities` 数组的元素数量 |
| `relations` | Integer | 固定输出 | `relations.jsonl` 中的关系数量 |
| `navigationNodes` | Integer | 固定输出 | `navigation/nodes.jsonl` 中的节点数量 |
| `navigationEdges` | Integer | 固定输出 | `navigation/edges.jsonl` 中的边数量 |

### 3.6 entityCounts

```json
{
  "FLOOR": 7,
  "SPACE": 864,
  "FACILITY": 94,
  "CONNECTOR": 57,
  "NAVIGATION_PATH": 1626
}
```

| 类型 | 含义 |
| --- | --- |
| `FLOOR` | 楼层实体 |
| `SPACE` | 店铺、房间、公共区域等空间实体 |
| `FACILITY` | POI 和设施实体 |
| `CONNECTOR` | 电梯、楼梯、扶梯、坡道等跨层设施实体 |
| `NAVIGATION_PATH` | 楼层内导航路径实体 |

### 3.7 relationCounts

```json
{
  "CONTAINS": 7,
  "LOCATED_ON": 2641,
  "LOCATED_IN": 94,
  "CONNECTED_TO": 131,
  "CONNECTS_FLOOR": 174,
  "SERVES_SPACE": 0
}
```

| 类型 | 含义 |
| --- | --- |
| `CONTAINS` | 地图包含楼层 |
| `LOCATED_ON` | 实体位于某一楼层 |
| `LOCATED_IN` | 点状设施位于某一空间内部 |
| `CONNECTED_TO` | 跨层设施之间属于同一连接组 |
| `CONNECTS_FLOOR` | 跨层设施连接目标楼层 |
| `SERVES_SPACE` | 导航路径服务指定空间 |

### 3.8 files

`files` 不包含 `manifest.json` 自身。每个元素采用以下结构：

```json
{
  "path": "entities.json",
  "mediaType": "application/json",
  "recordCount": 2648,
  "sizeBytes": 2013097,
  "sha256": "71b723f28fea9f09bf8ff9612e11d88bf7930d207fceabb395c514f7be5505fd"
}
```

| 字段 | 类型 | 输出规则 | 含义 |
| --- | --- | --- | --- |
| `path` | String | 固定输出 | 相对于地图包根目录的文件路径，使用 `/` 分隔 |
| `mediaType` | String | 固定输出 | 文件媒体类型 |
| `recordCount` | Integer | 固定输出 | 文件包含的逻辑记录数量 |
| `sizeBytes` | Integer | 固定输出 | 文件的实际字节数 |
| `sha256` | String | 固定输出 | 文件内容的 SHA-256，小写 64 位十六进制字符串 |

文件媒体类型和记录口径：

| 文件 | `mediaType` | `recordCount` 口径 |
| --- | --- | --- |
| `entities.json` | `application/json` | `entities` 数组的元素数量 |
| `relations.jsonl` | `application/x-ndjson` | 非空 JSON 记录行数 |
| `navigation/nodes.jsonl` | `application/x-ndjson` | 非空 JSON 记录行数 |
| `navigation/edges.jsonl` | `application/x-ndjson` | 非空 JSON 记录行数 |

`sizeBytes`、`sha256` 和 `recordCount` 与实际文件内容一致。

## 4. entities.json

### 4.1 文件作用与整体结构

`entities.json` 保存地图中的语义实体及其几何，包括楼层、空间、设施、跨层连接点和导航路径。

```json
{
  "entities": [
    {
      "id": "space:911907:F1:10001",
      "kind": "SPACE",
      "subtype": "STORE",
      "floorName": "F1",
      "name": "示例店铺",
      "typeCode": "120100",
      "labelPoint": {
        "type": "Point",
        "coordinates": [12958012.34, 4852643.21]
      },
      "geometry": {
        "type": "Polygon",
        "coordinates": [[
          [12958000.00, 4852630.00],
          [12958024.00, 4852630.00],
          [12958024.00, 4852656.00],
          [12958000.00, 4852656.00],
          [12958000.00, 4852630.00]
        ]]
      },
      "semanticProperties": {
        "access": "REACHABLE_ONLY",
        "typeLabel": "店铺",
        "categoryPath": ["商业空间", "店铺"]
      }
    }
  ]
}
```

`entities` 固定输出；没有实体时为空数组。数组按实体 `id` 升序排列，以保证相同输入产生稳定结果。

该文件使用普通 JSON，而不是 RFC 7946 GeoJSON。几何对象沿用 GeoJSON Geometry 的表达形式，但坐标采用本文件包声明的 EPSG:3857 米制平面坐标。

### 4.2 实体公共字段

| 字段 | 类型 | 输出规则 | 适用范围 | 含义 |
| --- | --- | --- | --- | --- |
| `id` | String | 固定输出 | 全部实体 | 实体语义 ID，也是关系引用该实体时使用的唯一标识 |
| `kind` | String | 固定输出 | 全部实体 | 实体大类，见 4.3 |
| `subtype` | String | 固定输出 | 全部实体 | 实体细分类型，例如店铺、电梯或步行路径 |
| `floorName` | String | 固定输出 | 全部实体 | 实体所在楼层名称，与 `manifest.floors[].name` 对应 |
| `name` | String | 有值时输出 | 可命名实体 | 中文名称、品牌名称或设施名称；空字符串不输出 |
| `englishName` | String | 有值时输出 | 可命名实体 | 英文名称；空字符串不输出 |
| `typeCode` | String | 有值时输出 | `SPACE`、`FACILITY` | 来源分类代码，仅作为分类标识，不用于表示方向或通行规则 |
| `labelPoint` | Geometry&lt;Point&gt; | 有值时输出 | 具有标注点的实体 | 推荐的文字或图标标注位置；不表示入口、接入点或几何质心 |
| `geometry` | Geometry | 固定输出 | 全部实体 | 实体的有效、非空主体几何，规则见 4.4 |
| `semanticProperties` | Object | 固定输出 | 全部实体 | 随 `kind` 变化的业务语义属性；没有扩展属性时为空对象 |

地图标识由目录和 `manifest.mapId` 提供。实体使用 `id` 作为唯一语义标识。实体结构不包含地图 ID 和来源绑定字段。

### 4.3 实体类型

| `kind` | 含义 | `semanticProperties` 字段 |
| --- | --- | --- |
| `FLOOR` | 楼层轮廓实体 | `gid` |
| `SPACE` | 店铺、房间、公共区域等空间实体 | `access`，以及有值时输出的 `typeLabel`、`categoryPath` |
| `FACILITY` | POI 和设施实体 | 有值时输出的 `typeLabel`、`categoryPath` |
| `CONNECTOR` | 电梯、楼梯、扶梯、坡道等跨层连接点 | `direction`、`accessible` |
| `NAVIGATION_PATH` | 楼层内导航路径实体 | `rank`、`direction`、`traversable`、`length`、`navigationMode` |

不同实体类型的字段规则如下：

- `FLOOR.semanticProperties.gid` 与 `manifest.floors[].gid` 含义一致；楼层别名仅在 `manifest.floors` 中表达。
- `SPACE.semanticProperties.access` 表示空间的通行参与方式，取值为 `TRAVERSABLE`、`REACHABLE_ONLY`、`OBSTACLE`、`DISPLAY_ONLY` 或 `UNKNOWN`。
- `CONNECTOR.semanticProperties.direction` 取值为 `BIDIRECTIONAL`、`UP_ONLY`、`DOWN_ONLY`、`CLOSED` 或 `UNKNOWN`。
- `CONNECTOR.semanticProperties.accessible` 为 Boolean。源数据值为 `1` 时输出 `true`；值为 `0`、字段不存在或值为空时输出 `false`。
- 跨层设施的分组及连接楼层通过 `CONNECTED_TO` 和 `CONNECTS_FLOOR` 关系表达，不在实体中重复保存。
- `NAVIGATION_PATH.semanticProperties.rank` 表示路径等级，`direction` 表示通行方向，`traversable` 表示是否可通行，`length` 表示路径有效长度，单位为米，`navigationMode` 表示路径支持的导航模式。
- 导航路径与空间的服务关系通过 `SERVES_SPACE` 表达，不在实体中重复保存空间 ID 列表。

### 4.4 几何对象

几何对象采用以下结构：

```json
{
  "type": "Point",
  "coordinates": [12958012.34, 4852643.21]
}
```

约束如下：

- 坐标只包含 X、Y 两个维度；
- X、Y 坐标采用 EPSG:3857，单位为米；
- 支持 `Point`、`LineString`、`MultiLineString`、`Polygon` 和 `MultiPolygon`；
- `FLOOR` 的主体几何统一为 `Polygon` 或 `MultiPolygon`；
- `geometry` 为有效且非空的主体几何；
- `labelPoint` 只使用 `Point`。

### 4.5 分类字段

分类信息由以下字段共同表达：

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `typeCode` | String | 分类代码，用于稳定匹配分类定义 |
| `semanticProperties.typeLabel` | String | 当前实体采用的分类名称 |
| `semanticProperties.categoryPath` | Array&lt;String&gt; | 从上级到下级的分类路径 |

## 5. relations.jsonl

### 5.1 文件作用与记录结构

`relations.jsonl` 表达地图、楼层和实体之间的确定性语义关系。文件采用 JSONL 格式，每个非空行是一条完整关系记录：

```json
{"id":"relation:LOCATED_IN:facility:911907:B1:911907990210:space:911907:B1:911907990188","type":"LOCATED_IN","sourceId":"facility:911907:B1:911907990210","targetId":"space:911907:B1:911907990188"}
```

| 字段 | 类型 | 输出规则 | 含义 |
| --- | --- | --- | --- |
| `id` | String | 固定输出 | 稳定且唯一的关系 ID |
| `type` | String | 固定输出 | 关系类型，见 5.2 |
| `sourceId` | String | 固定输出 | 关系语义上的起点 ID；不表示来源数据 ID |
| `targetId` | String | 固定输出 | 关系语义上的终点 ID |

关系 ID 由关系类型和两个端点 ID 确定。相同类型、相同起点和相同终点只输出一条关系。记录按 `id` 升序排列；没有关系时文件内容为空。

### 5.2 关系类型与端点

| `type` | `sourceId` 对象 | `targetId` 对象 | 含义 |
| --- | --- | --- | --- |
| `CONTAINS` | 地图语义 ID | `FLOOR` | 地图包含楼层 |
| `LOCATED_ON` | `SPACE`、`FACILITY`、`CONNECTOR` 或 `NAVIGATION_PATH` | `FLOOR` | 实体位于目标楼层 |
| `LOCATED_IN` | `FACILITY` | `SPACE` | 设施位于空间内部或边界上 |
| `CONNECTED_TO` | `CONNECTOR` | `CONNECTOR` | 不同楼层的两个跨层连接点属于同一可换乘连接 |
| `CONNECTS_FLOOR` | `CONNECTOR` | `FLOOR` | 跨层连接点声明连接到目标楼层 |
| `SERVES_SPACE` | `NAVIGATION_PATH` | `SPACE` | 导航路径为目标空间提供导航接入 |

## 6. navigation/nodes.jsonl

### 6.1 文件作用与记录结构

`navigation/nodes.jsonl` 保存可计算导航图中的节点。文件采用 JSONL 格式，每个非空行是一条完整节点记录：

```json
{"id":"nav-node:911907:B1:WALK:12965657548:4852697366","floorName":"B1","mode":"WALK","geometry":{"type":"Point","coordinates":[12965657.548,4852697.366]},"pathIds":["path:911907:B1:PATH.142"],"connectorIds":["connector:911907:B1:LIFT.4"]}
```

| 字段 | 类型 | 输出规则 | 含义 |
| --- | --- | --- | --- |
| `id` | String | 固定输出 | 稳定且唯一的导航节点 ID |
| `floorName` | String | 固定输出 | 节点所在楼层，与 `manifest.floors[].name` 对应 |
| `mode` | String | 固定输出 | 导航模式，见 6.2 |
| `geometry` | Geometry&lt;Point&gt; | 固定输出 | 节点位置，采用 EPSG:3857 二维坐标 |
| `pathIds` | Array&lt;String&gt; | 固定输出 | 在该节点连接的导航路径实体 ID，至少包含一项 |
| `connectorIds` | Array&lt;String&gt; | 有值时输出 | 在该节点接入导航图的跨层连接点实体 ID |
| `accessSpaceIds` | Array&lt;String&gt; | 有值时输出 | 通过该节点接入导航图的空间实体 ID |

记录按 `id` 升序排列。没有可导航路径时文件内容为空。

地图标识由目录和 `manifest.mapId` 提供。节点结构不包含地图 ID、节点类型、拓扑构建诊断字段和来源绑定字段。

### 6.2 导航模式

| `mode` | 含义 | 数据来源 |
| --- | --- | --- |
| `WALK` | 普通步行路网 | `PATH` |
| `ACCESSIBLE` | 明确标识为无障碍通行的路网 | `PATH-ACCESSIBLE` |
| `DRIVE` | 车行路网 | `PATH-DRIVE` |

### 6.3 节点角色

| 条件 | 节点角色 |
| --- | --- |
| `pathIds` 非空 | 路径端点、路径交点或路径切分点 |
| `connectorIds` 非空 | 电梯、楼梯、扶梯或坡道等跨层连接点 |
| `accessSpaceIds` 非空 | 空间的导航接入点 |

### 6.4 引用与几何

- `pathIds` 引用 `entities.json` 中同楼层的 `NAVIGATION_PATH` 实体；
- `connectorIds` 引用 `entities.json` 中同楼层的 `CONNECTOR` 实体；
- `accessSpaceIds` 引用 `entities.json` 中同楼层的 `SPACE` 实体；

## 7. navigation/edges.jsonl

### 7.1 文件作用

`navigation/edges.jsonl` 保存可计算导航图中的有向边。文件采用 JSONL 格式，每个非空行是一条 `PATH` 或 `TRANSFER` 边，记录按 `id` 升序排列。没有导航边时文件内容为空。

公共字段：

| 字段 | 类型 | 输出规则 | 含义 |
| --- | --- | --- | --- |
| `id` | String | 固定输出 | 稳定且唯一的导航边 ID |
| `fromNodeId` | String | 固定输出 | 有向边起点，引用 `navigation/nodes.jsonl` 中的节点 |
| `toNodeId` | String | 固定输出 | 有向边终点，引用 `navigation/nodes.jsonl` 中的节点 |
| `mode` | String | 固定输出 | `WALK`、`ACCESSIBLE` 或 `DRIVE` |
| `type` | String | 固定输出 | `PATH` 或 `TRANSFER` |

### 7.2 PATH 边

```json
{"id":"nav-edge:path:911907:B1:PATH.100:0:0:WALK:F","fromNodeId":"nav-node:911907:B1:WALK:12965698338:4852601467","toNodeId":"nav-node:911907:B1:WALK:12965714467:4852601223","mode":"WALK","type":"PATH","pathId":"path:911907:B1:PATH.100","length":16.131,"geometry":{"type":"LineString","coordinates":[[12965698.338,4852601.467],[12965714.467,4852601.223]]}}
```

| 字段 | 类型 | 输出规则 | 含义 |
| --- | --- | --- | --- |
| `pathId` | String | 固定输出 | 产生该边的 `NAVIGATION_PATH` 实体 ID |
| `length` | Number | 固定输出 | 边几何长度，单位为米，精确到 0.001 米 |
| `geometry` | Geometry&lt;LineString&gt; | 固定输出 | 与边方向一致的 EPSG:3857 二维线几何 |

PATH 边规则：

- 起点和终点位于同一楼层且导航模式相同；
- `fromNodeId` 与 `toNodeId` 不相同；
- LineString 首尾坐标分别与起点、终点节点坐标一致；
- `length` 根据输出 LineString 计算；
- `pathId` 引用同楼层、模式匹配的 `NAVIGATION_PATH`；
- `BIDIRECTIONAL` 路径形成方向相反的两条边；
- `FORWARD_ONLY` 和 `REVERSE_ONLY` 只形成对应方向的边；
- `CLOSED` 和 `UNKNOWN` 不形成导航边。

### 7.3 TRANSFER 边

```json
{"id":"nav-edge:transfer:connector:911907:B1:ESCALATOR.1:connector:911907:F1:ESCALATOR.1:WALK","fromNodeId":"nav-node:911907:B1:WALK:12965683150:4852622382","toNodeId":"nav-node:911907:F1:WALK:12965682662:4852623415","mode":"WALK","type":"TRANSFER","fromConnectorId":"connector:911907:B1:ESCALATOR.1","toConnectorId":"connector:911907:F1:ESCALATOR.1"}
```

| 字段 | 类型 | 输出规则 | 含义 |
| --- | --- | --- | --- |
| `fromConnectorId` | String | 固定输出 | 起点楼层的 Connector 实体 ID |
| `toConnectorId` | String | 固定输出 | 目标楼层的 Connector 实体 ID |

TRANSFER 边表达跨楼层换乘，不输出平面 `geometry`、`length` 或通用 `cost`。两个楼层上的平面点连线不表示跨层设施的真实三维轨迹。

TRANSFER 边同时满足：

- 两个 Connector 之间存在 `CONNECTED_TO`；
- 起点 Connector 存在指向目标楼层的 `CONNECTS_FLOOR`；
- 两个 Connector 均已接入相同模式的楼层内 PATH；
- 起点 Connector 的 `direction` 允许向目标楼层移动；
- `ACCESSIBLE` 模式下使用明确的无障碍 PATH，且两个 Connector 的 `accessible` 均为 `true`；
- 起点和终点位于不同楼层；
- `fromConnectorId` 属于起点节点的 `connectorIds`，`toConnectorId` 属于终点节点的 `connectorIds`。

`CONNECTED_TO` 表示 Connector 属于同一连接组，不单独作为组内任意楼层直达的依据。电梯、扶梯、楼梯和坡道实际连接的目标楼层以 `CONNECTS_FLOOR` 为准。
